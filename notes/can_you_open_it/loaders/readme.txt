To reduce the reverse workload, here is the binary and source code of all loaderX files in /home/levelX/.
Use the password of each level to uncompress the zip files. (Do not use unzip on Linux, use 7z instead)

The `seccomp-tools dump ./loaderX` command can show the seccomp rules that prctl sets. 
(https://github.com/david942j/seccomp-tools)

